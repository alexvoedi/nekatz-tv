{
  "name": "backend",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint . --cache --concurrency=1",
    "lint:fix": "eslint . --fix --concurrency=1",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "catalog:",
    "express": "catalog:",
    "fluent-ffmpeg": "catalog:"
  },
  "devDependencies": {
    "@types/cors": "catalog:",
    "@types/express": "catalog:",
    "@types/fluent-ffmpeg": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
